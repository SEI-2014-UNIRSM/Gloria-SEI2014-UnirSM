void setup() {
 size(displayWidth, displayHeight);
 background(0);
}


void draw() {
 stroke(255);
 strokeWeight(4);
 smooth();
 // disegna se clicco con il mouse
 if (mousePressed) {
   line(pmouseX, pmouseY, mouseX, mouseY);
 }
}
